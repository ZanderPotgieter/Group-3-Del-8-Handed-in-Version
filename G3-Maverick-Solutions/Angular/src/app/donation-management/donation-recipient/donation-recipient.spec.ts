import { DonationRecipient } from '../donation-management/donation-recipient';

describe('DonationRecipient', () => {
  it('should create an instance', () => {
    expect(new DonationRecipient()).toBeTruthy();
  });
});
