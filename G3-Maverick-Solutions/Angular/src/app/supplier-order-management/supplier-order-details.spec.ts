import { SupplierOrderDetails } from './supplier-order-details';

describe('SupplierOrderDetails', () => {
  it('should create an instance', () => {
    expect(new SupplierOrderDetails()).toBeTruthy();
  });
});
