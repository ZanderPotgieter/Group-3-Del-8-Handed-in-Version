import { SupplierOrderStatus } from './supplier-order-status';

describe('SupplierOrderStatus', () => {
  it('should create an instance', () => {
    expect(new SupplierOrderStatus()).toBeTruthy();
  });
});
